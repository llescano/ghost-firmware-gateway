# idf_component.yml - firmware_gateway
# Dependencias externas para el Gateway Central
# Se descargan automáticamente desde el Component Registry

dependencies:
  # LED RGB para indicación de estados visuales
  espressif/led_indicator: ">=2.1.1"
  # Parser JSON para protocolo de comunicación
  espressif/cjson: "*"
  # Componente de botones para uso futuro, se descargará si se requiere
  espressif/button: ">=4.1.5"
  # NOTA: esp_websocket_client ahora es un componente local
